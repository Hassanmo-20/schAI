# SchAI Frontend Environment Variables
# Base URL of the Laravel API (see backend/.env APP_URL).
VITE_API_BASE_URL=http://localhost:8000/api
# true  = run entirely on in-browser mock data, no backend required
# false = call the real Laravel API at VITE_API_BASE_URL
VITE_USE_MOCK_DATA=true
